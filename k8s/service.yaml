apiVersion: v1
kind: Service
metadata:
  name: trend-store
spec:
  selector:
    app: trend-store
  type: LoadBalancer
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
