apiVersion: v1
kind: Service
metadata:
  name: trend-store
spec:
  selector:
    app: trend-store
  type: LoadBalancer
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 80
